version: "3.9"

services:
  backend:
    build: ./backend
    container_name: backend
    ports:
      - "2800:2800"
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: frontend
    ports:
      - "5173:5173"
    restart: unless-stopped
